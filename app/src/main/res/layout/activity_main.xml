<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:theme="@style/Platform.MaterialComponents"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=" ListView/GridView Assignment 3"
        android:layout_marginTop="20dp"
        android:textColor="@color/black"
        android:layout_centerHorizontal="true"
        android:textSize="25dp"/>


    <Button
        android:id="@+id/btnListView"
        android:layout_width="190dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/titleText"
        android:hint="ListView"
        android:layout_centerHorizontal="true"
        android:textColorHint="@color/white"/>

    <Button
        android:id="@+id/btnListViewWithImage"
        android:layout_width="190dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnListView"
        android:hint="ListView with Image"
        android:layout_marginTop="50dp"
        android:textColorHint="@color/white"
        android:layout_centerHorizontal="true"
        />
    <Button
        android:id="@+id/btnGridView"
        android:layout_width="190dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnListViewWithImage"
        android:hint="GridView"
        android:layout_marginTop="50dp"
        android:textColorHint="@color/white"
        android:layout_centerHorizontal="true"
        />

</RelativeLayout>